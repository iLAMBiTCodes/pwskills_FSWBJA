// Comma(,) operator in JavaScript is an operator in which operands(expressions, values) are separated by commas and the expressions gets executed from left to right.
// Syntax: Expression1, Expression2, Expression3

let x = (2, 4, 67, 9+2);
console.log(x); //Output: 11